
/*show /show load def
/show { %def
	{ %forall
		/charcode exch def
		/thischar ( ) dup 0 charcode put def
		
		gsave
		 currentpoint
		 /currenty exch def
		 /currentx exch def
		 thischar true charpath
		 letter
		 paint
		grestore

		currentx thischar stringwidth pop add  currenty moveto
	} forall
} bind def

paint { %def
	fill
} bind def

letter { %def
} bind def		
